Execute ALL 13 pipeline phases in order. NEVER skip phases.

For EACH phase:
1. Call `pipeline-advance` with { phase: PHASE_NAME, status: "start" }
2. Delegate the work using `task` tool to the specified sub-agent
3. Call `pipeline-advance` with { phase: PHASE_NAME, status: "complete" }
4. Proceed immediately to next phase

Phases:
1. discovery  → task to explore agent
2. research   → task to general agent
3. planning   → task to planner agent
4. architecture → task to architect agent
5. debate     → task to general agent
6. implementation → task to general agent
7. review     → task to code-reviewer agent
8. qa         → task to qa agent
9. security   → task to security agent
10. self-critique → task to auditor agent
11. question  → task to questionador agent
12. audit     → task to auditor agent
13. delivery  → task to release-manager agent

IMPORTANT: Complete all phases. After each phase, announce it.
If a phase fails, fix and retry. Do not skip.
