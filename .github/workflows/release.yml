name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      channel:
        description: "Release channel"
        default: "dev"
        type: choice
        options:
          - dev
          - beta
          - prod

permissions:
  contents: write

env:
  OPENCODE_CHANNEL: ${{ inputs.channel || 'dev' }}

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Build Electron
        working-directory: packages/desktop
        run: |
          bun run prebuild
          bun run build

      - name: Package (Linux)
        working-directory: packages/desktop
        run: |
          npx electron-builder --linux --config electron-builder.config.ts

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: opencode-linux
          path: |
            packages/desktop/dist/*.AppImage
            packages/desktop/dist/*.deb
            packages/desktop/dist/*.rpm
            packages/desktop/dist/*.tar.gz
            packages/desktop/dist/latest-linux.yml

  build-windows:
    runs-on: windows-2025
    defaults:
      run:
        working-directory: packages/desktop
        shell: bash
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          export PATH="$HOME/.bun/bin:$PATH"
          bun install --frozen-lockfile

      - name: Build Electron
        run: |
          export PATH="$HOME/.bun/bin:$PATH"
          export OPENCODE_CHANNEL=${{ env.OPENCODE_CHANNEL }}
          bun run build

      - name: Package (Windows NSIS)
        run: |
          export PATH="$HOME/.bun/bin:$PATH"
          export OPENCODE_CHANNEL=${{ env.OPENCODE_CHANNEL }}
          npx electron-builder --win --config electron-builder.config.ts

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: opencode-windows
          path: |
            packages/desktop/dist/*.exe
            packages/desktop/dist/*.yml
            packages/desktop/dist/*.blockmap

  create-release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "InnexarCode ${{ github.ref_name }}"
          files: |
            artifacts/**/*
          generate_release_notes: true
          prerelease: ${{ env.OPENCODE_CHANNEL != 'prod' }}
          token: ${{ secrets.GH_TOKEN }}
